[pytest]
markers =
    pet: Tests related to the pet API.
    store: Tests related to the store API.
    user: Tests related to the user API.
addopts = --tb=short --cache-clear --junitxml=reports/test_report.xml # Refer to README.md for other command line options
pythonpath = src
testpaths = tests
filterwarnings = ignore::Warning
